## Day 2

### Challenge

Initially misunderstood the purpose of `cd ..`, assuming it navigated directly to the root directory.

### Resolution

Learned the difference between:

- `cd ..` → Move to the parent directory.
- `cd ~` → Return to the home directory.
- `cd /` → Navigate directly to the root directory.

This clarified how Linux directory navigation works.
